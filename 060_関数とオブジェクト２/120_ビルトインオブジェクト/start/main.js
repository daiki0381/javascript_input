/*
  ・コード実行前にJSエンジンによって自動的に生成されるオブジェクトを「ビルトインオブジェクト」という。
  ・「ビルトインオブジェクト」には「String」「Object」「Array」「Function」「Boolean」「Symbol」等がある。
*/

const array = new Array(1, 2, 3, 4);

array[0];
array.hasOwnProperty(0);
